TITLE='Read a GPX file with no points'
PRECOMMAND='cat "$STAGINGDIR/point1-1.jpg" >"$LOGDIR/test.jpg"'
COMMAND='$PROGRAM -z 0 -g "$STAGINGDIR/nopoints.gpx" "$LOGDIR/test.jpg" > "$OUTFILE" 2>&1'
POSTCOMMAND='rm -f "$LOGDIR/test.jpg"'
RESULTCODE=2
